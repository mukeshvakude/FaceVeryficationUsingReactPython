PORT=4000
USERS_CSV=./data/users.csv
ADMIN_EMAIL=admin@example.com
JWT_SECRET=replace_with_strong_secret
AES_SECRET=replace_with_32_char_or_longer_secret
FACE_SERVICE_URL=http://localhost:5001/verify-face
CLIENT_ORIGIN=http://localhost:5173

# ===== DATABASE CONFIGURATION =====
# Choose either MySQL or PostgreSQL
# The system auto-detects based on host domain or port

# --- OPTION 1: MySQL (Local Development) ---
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DB=face_verification_db

# --- OPTION 2: PostgreSQL (Supabase/Render - Production) ---
# Supports multiple variable naming formats:
# Format 1 (Render style):
DATABASE_HOST=ap-south-2.pg.psdb.cloud
DATABASE_PORT=5432
DATABASE_NAME=postgres
DATABASE_USERNAME=postgres.xxxxx
DATABASE_PASSWORD=your_password

# Format 2 (Alternative):
# DB_HOST=ap-south-2.pg.psdb.cloud
# DB_PORT=5432
# DB_NAME=postgres
# DB_USER=postgres.xxxxx
# DB_PASS=your_password

# ===== EMAIL CONFIGURATION =====
# For sending stego images with encryption keys via email

# --- OPTION 1: SendGrid API (RECOMMENDED for Production/Render) ---
# SendGrid works on Render Free Tier (bypasses SMTP port restrictions)
# Get your API key: https://app.sendgrid.com/settings/api_keys
# 1. Sign up at sendgrid.com (free tier: 100 emails/day)
# 2. Create API key with "Mail Send" permissions
# 3. Verify sender email in SendGrid dashboard
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SENDGRID_FROM_EMAIL=your-verified-email@example.com

# --- OPTION 2: SMTP (Gmail/Nodemailer) - Local Development Only ---
# Gmail: Use App Password (not your regular Gmail password)
# Visit: https://myaccount.google.com/apppasswords
# Note: SMTP may not work on Render Free Tier due to port restrictions
EMAIL_SERVICE=gmail
EMAIL_USER=rohanpk021204@gmail.com
EMAIL_PASS=ilewjehtluykisaj
# Alternative SMTP configuration:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false

